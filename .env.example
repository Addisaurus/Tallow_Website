# Environment Variables Template for Tallow & Co.
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control!

# ============================================
# Flask Configuration
# ============================================
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-generate-new-one

# Flask Environment (use 'production' for deployment)
FLASK_ENV=production

# ============================================
# Database Configuration
# ============================================
# For Docker/Railway/Render, these will be auto-configured
# Only needed if manually setting up database
POSTGRES_DB=tallow_db
POSTGRES_USER=tallow_user
POSTGRES_PASSWORD=your-strong-password-here

# Database URL - Railway/Render will provide this automatically
# Format: postgresql://username:password@hostname:5432/database
# Leave empty for Railway/Render (they inject DATABASE_URL)
DATABASE_URL=

# ============================================
# Stripe Configuration - PRODUCTION (LIVE) KEYS
# ============================================
# Get your LIVE keys from: https://dashboard.stripe.com/apikeys
# ⚠️ WARNING: Use pk_live_... and sk_live_... for production!
# Do NOT use test keys (pk_test_...) in production!

STRIPE_PUBLISHABLE_KEY=pk_live_your_publishable_key_here
STRIPE_SECRET_KEY=sk_live_your_secret_key_here

# Stripe Webhook Secret
# Get this from: Stripe Dashboard → Developers → Webhooks
# Create endpoint: https://yourdomain.com/webhook
# Select events: checkout.session.completed, payment_intent.succeeded
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# ============================================
# Email Configuration (Optional)
# ============================================
# For sending order confirmations and contact form emails
# Gmail example (requires App Password if using Gmail):
# https://support.google.com/accounts/answer/185833

MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=your-email@example.com
MAIL_PASSWORD=your-app-password-here

# ============================================
# Production Deployment Checklist
# ============================================
# Before deploying to production, ensure:
# ✓ Generated new SECRET_KEY (never reuse development key)
# ✓ Using Stripe LIVE keys (pk_live_... and sk_live_...)
# ✓ Set FLASK_ENV=production
# ✓ Strong POSTGRES_PASSWORD if self-hosting database
# ✓ Configured Stripe webhook and added secret
# ✓ Email settings configured (if using contact form)
# ✓ This .env.example is committed to git
# ✓ Actual .env file is NOT committed (check .gitignore)
